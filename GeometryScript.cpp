#include "GeometryScript.h"
#include "BuildingPrimitive.h"
// Include necessary headers for geometry scripting

void UGeometryScript::AddWindow(ABuildingPrimitive* TargetPrimitive, FVector2D Position, FVector2D Size)
{
    if (!TargetPrimitive) return;

    // Implement geometry scripting logic to add a window
    // This may involve modifying the mesh using procedural mesh components
}

void UGeometryScript::AddDoor(ABuildingPrimitive* TargetPrimitive, FVector2D Position, FVector2D Size)
{
    if (!TargetPrimitive) return;

    // Implement geometry scripting logic to add a door
}
